# Therapists — Current State

## Implemented (DB level)

`public.therapists`: `id`, `name`, `archived`/`archived_at`/`archived_by`/
`archived_reason` (soft-archive pattern, not hard delete).

Availability/scheduling support tables:
- `therapist_absence` (one-off dated absences)
- `therapist_leave` (date-range leave, `end_date >= start_date` checked)
- `therapist_day_off` (recurring weekday off, `weekday` 0–6 checked)
- `therapist_services` (join table: which services a therapist can perform)

## Implemented (app level)

- `app/dashboard/page.tsx` reads a live count of `therapists` where `archived = false` ("Available Therapists" card).
- `app/(staff)/therapists/page.tsx` (`TherapistBrowser` component,
  `components/therapist-browser.tsx`) implements the full HTML mockup
  parity:
  - Default 10 therapists matching mockup (`Ron`, `Don`, `Tristan`, `Leo`, `Roy`, `Xander`, `Dan`, `Marco`, `Akio`, `Josh`) — only used as a fallback if the live `therapists` table is empty; all 10 exist live.
  - Interactive Date (defaults to the device's real local date, via a local-time `todayISO()` — not `toISOString()`, which is UTC and drifts a day off near local midnight), Time (`16:00` to `01:00`), and Availability filters (`Select All`, `Available`, `Booked`), plus `Show Archived` toggle.
  - **Weekly Day(s) Off toggle pills (`Sun`–`Sat`) — real Supabase persistence** (`ohm#7k2m9x4p`, 2026-08-30): writes through `app/(staff)/therapists/actions.ts` (`toggleDayOff`) to `therapist_day_off`, keyed by real therapist `id` (component holds a `therapistIds`: name→id map alongside the existing name-keyed `therapistMeta`, updated on rename). Page-level fetch now selects `therapists.id` (not just `name`) and joins `therapist_day_off` to seed initial state. RLS: `staff_select`/`staff_insert`/`staff_delete` on `therapist_day_off` (`is_staff()` read, `is_supervisor_or_above()` write) — see RLS section below, this table had **no policies at all** before this fix.
  - **Services Offered toggle pills (`Combi Massage`, `Signature Massage`, `Scrub`) — real Supabase persistence, and the per-card UI itself newly built** (`ohm#9q4x1mwr`, 2026-09-01; persistence bug fixed & booking filter wired `ohm#4a8d2f1b`, 2026-09-19; service name/ID mismatch resolved `ohm#1b4e9f7a`, 2026-09-21). Investigation surfaced a discrepancy from the prompt's premise: `handleToggleService` existed and was genuinely local-state-only as expected, but it was never called from any JSX — there was **no "Services Offered" section on the therapist card at all** prior to this prompt (the only place the three service pills rendered was the separate, still-local `addServices` picker inside the Add Therapist modal). Approved: build the missing card section rather than assume it already existed. **Service Name vs ID Resolution (`ohm#1b4e9f7a`, 2026-09-21)**: Catalog contains inactive `"Scrub"` (`326e0b78-49cb-441c-aa03-e54453f2f67f`, `active: false`, ₱900) and active `"Scrub + Massage"` (`8c97c5db-eaa9-47b9-89c0-9db114000483`, `active: true`, ₱1,800). The card pill was concise `"Scrub"`, which previously resolved to the inactive ID and prevented scrub services from appearing in booking modals. Fixed by migrating `therapist_services` rows to the active `"Scrub + Massage"` UUID, resolving `"Scrub"` in `serviceIdMap` to `serviceIds["Scrub + Massage"] ?? serviceIds["Scrub"]`, normalizing server-loaded `"Scrub + Massage"` strings to `"Scrub"` on card display/sync (`normalizeServiceNames`), and adding defense-in-depth alias normalization in `QuickWalkinModal`. New "Services Offered" pill section added to each therapist card (same visual pattern as Weekly Day(s) Off), wired to `handleToggleService` (now async) → new server action `toggleTherapistService(therapistId, serviceId, offering, staffId)` in `app/(staff)/therapists/actions.ts`, mirroring `toggleDayOff`'s plain insert-on/delete-off shape. Component now holds a `serviceIdMap` (service name → `services.id`) seeded from a new `serviceIds` prop. Page-level fetch now also selects `services (id, name)` and joins `therapist_services` to seed a new `initialServices` prop (`therapist_id → service names[]`), closing the same "refresh silently loses state" gap already closed for day-off/absence/leave/archive. **Persistence bug fixed (`ohm#4a8d2f1b`, 2026-09-19)**: `TherapistBrowser` state initialization previously fell back to `restricted[r.name]` or `ALL_THERAPIST_SERVICES.slice()` whenever `r.id in initialServices` was false (e.g. when a therapist had all services unselected), causing services to reset on page reload; fixed by initializing to `initialServices[r.id] ?? []` when real DB records are loaded. `handleToggleService` now calls `router.refresh()`, and `toggleTherapistService` action uses resilient service role client mutation fallback (avoiding RLS silent zero drops on DELETE for non-supervisors) and revalidates `/therapists` + `/bookings`. Booking modals (`QuickWalkinModal`, `BookingFormModal`) now strictly filter therapists by `therapist_services`. RLS: `staff_select`/`staff_insert`/`staff_delete` on `therapist_services` (`is_staff()` read, `is_supervisor_or_above()` write, no UPDATE — pure join table, PK `(therapist_id, service_id)`).
  - Kebab dropdown menu on each therapist card supporting `Mark Absent Today`, `Mark On Leave`, `Archive`, `Unarchive`, and `Edit` (rename) — **fixed** (`ohm#7k2m9x4p`, 2026-08-30): the menu's actions themselves were already correctly wired, but a `document`-level click-outside-to-close listener closed the menu in the same tick it opened, because it and React's own delegated click listener are both attached to `document` in this React/Next version — a sibling listener on the same node isn't stopped by `e.stopPropagation()`. Fixed by having the close listener explicitly skip clicks that land inside a `data-kebab-root`-marked wrapper instead of relying on `stopPropagation()`. At the time, `Archive`/`Unarchive`/`Edit` (plus Add Therapist) were all local-state only — see below for what's since changed.
  - **`Mark Absent Today` / `Mark On Leave` — real Supabase persistence + Dashboard reassignment trigger** (`ohm#3f8q1w6z`, 2026-08-30): `handleMarkAbsent`/`handleConfirmLeave` now call new server actions `markAbsentToday()`/`markOnLeave()` (`app/(staff)/therapists/actions.ts`) instead of only mutating local state. `markAbsentToday` upserts `therapist_absence` (ignoring the unique `(therapist_id, absent_date)` conflict if already marked) then flags that therapist's same-day `Booked` bookings to `Needs Reassignment`; `markOnLeave` inserts `therapist_leave` then flags `Booked` bookings across the leave's date range the same way. Both log one `action_logs` row and revalidate `/therapists` + `/dashboard`. `app/(staff)/therapists/page.tsx` now also fetches `therapist_absence`/`therapist_leave` and seeds `TherapistBrowser`'s `initialAbsence`/`initialLeave` props (previously always empty on load — a page refresh silently lost the "who's absent/on leave" state even before this fix, since nothing was ever persisted). Flagged bookings then surface on `/dashboard` with a Transfer action — see [[dashboard_state]].
  - **`Archive` / `Unarchive` — real Supabase persistence** (`ohm#7m2w9dxk`, 2026-09-01; updated `ohm#arcreassgn`, 2026-09-17): `handleConfirmArchive`/`handleUnarchive` call server actions `archiveTherapist()`/`unarchiveTherapist()` (`app/(staff)/therapists/actions.ts`). `archiveTherapist` updates `therapists.archived`/`archived_reason`/`archived_by`/`archived_at`, then flags that therapist's `Booked` bookings with `booking_date >= current_spa_date` (`spaDayNow()`) to `Needs Reassignment` — strictly guarded by `current_spa_date` so past historical bookings remain untouched (`ohm#arcreassgn`). `unarchiveTherapist` clears those four columns; it does not un-flag any booking. Neither action touches `therapist_day_off` or `therapist_services`. `app/(staff)/therapists/page.tsx` fetches `therapists.archived`/`archived_reason`/`archived_at` and seeds `TherapistBrowser`'s `initialArchived` prop.
  - Add Therapist modal, Daily Schedule modal, Mark On Leave modal, Archive modal, and Edit Name modal with toast feedback. Daily Schedule modal remains local-state only, unchanged.
  - **Add Therapist / Edit (rename) — real Supabase persistence** (`ohm#5v8n3ptc`, 2026-09-01): `handleConfirmAdd` calls `createTherapist()` (`app/(staff)/therapists/actions.ts`), which was already wired but previously **silently RLS-rejected** — `therapists` had no INSERT policy, so the insert failed and the UI showed an error toast instead of adding the therapist. Fixed by adding a `staff_insert` (`is_supervisor_or_above()`) RLS policy — no client-side code change was needed for Add itself. `handleConfirmEdit` (the Edit Name modal, name-only field — no position/comment exposed) now calls a new server action `updateTherapistName(therapistId, name, staffId)` (`UPDATE therapists SET name = … WHERE id = …`, `action_logs` insert as `therapist_rename`, `revalidatePath("/therapists")`) instead of only mutating local state; reuses the existing `staff_update` policy from `ohm#7m2w9dxk`, no new policy needed. No uniqueness check on `therapists.name` — confirmed live there's no unique constraint (only the PK on `id`); the pre-existing client-side duplicate-name guard is unchanged. Confirmed the component's name-keyed `therapistIds`/`therapistMeta` maps aren't referenced as an identifier anywhere outside `therapist-browser.tsx` — other code (e.g. `app/(staff)/bookings/actions.ts`) always looks up therapists by `id`, using `name` only for human-readable log strings — so rename carries no collision risk.
  - **Copy Available-List to clipboard** (`ohm#9d4r7t2h`, 2026-08-30): a copy-icon button next to "Show Archived" runs `handleCopyAvailable`, which filters the existing `cardRows` (already computed per-card `slotStatus`, respecting day-off/leave/absence/archive/busy logic — no new filtering added) for `slotStatus === "available"`, and copies `"{fmtTime(viewTime)} Available\n\n{Name}\n..."` to the clipboard via `navigator.clipboard.writeText`, confirmed with the existing toast. Client-only, no persistence, no backend involvement.
  - **Interactive Slot Selection on Therapist Cards & Pre-filled Quick Walk-in Modal** (`ohm#2d9a4b8f`, 2026-09-19): `TherapistBrowser` tracks `selectedSlotByTherapist: Record<string, string>`, passed along with `onSelectSlot` to `TherapistCard`. Clicking an available slot pill selects that slot on the card (`bg-foreground text-background font-semibold`) and dynamically updates the card's action button to `Book {fmtTime(activeSlot)}` (e.g. `Book 10:00 PM`). Past, booked, and unavailable slots remain non-clickable. Clicking `Book [Time]` opens `<QuickWalkinModal />` directly on the roster, pre-filled with the selected therapist, chosen slot time, and active `viewDate`. `QuickWalkinModal` automatically retains the therapist without clearing, resolves the first qualified massage service offered by the therapist, and auto-derives the first free room for that slot.
  - **Light Mode Theme Support** (`ohm#8e2c4b1d`, 2026-09-19): All hardcoded dark-theme hex colors in `TherapistCard` and remaining ones in `TherapistBrowser` replaced with semantic Tailwind CSS theme tokens (`bg-surface`, `border-border`, `text-foreground`, `text-muted`, `bg-accent-red`, `bg-accent-green`, `bg-surface-2`, etc.). `--gold` and `--gold-hover` CSS variables added to `body.light` in `globals.css`. Cards now adapt cleanly to both light and dark mode. Zero hardcoded hex values remain in either file.
  - **Confirmation Modal for Weekly Days Off & Services Offered Changes** (`ohm#9d3b7e1a`, 2026-09-21): Toggling day-off or services-offered pills on a therapist card intercepts immediate mutation execution and opens a confirmation modal (`BadgeConfirmState`). Displays therapist name, target action description (`"Add/Remove [Day] as/from weekly day off"`, `"Add/Remove [Service] to/from services offered"`), and warns when adding a day off that future active bookings on that weekday will be flagged as `Needs Reassignment`. Cancelling leaves state intact with zero network calls; confirming executes the server mutation (`toggleDayOff`, `toggleTherapistService`), updates local `therapistMeta`, triggers revalidation, and presents toast notification. Fully styled with semantic theme tokens (`bg-surface`, `bg-surface-2`, `border-border`, `text-foreground`, `text-muted`, `bg-gold`).

## Availability enforcement in Bookings

`therapist_day_off`/`therapist_absence`/`therapist_leave` are now enforced
as a DB-level gate on `bookings` (trigger `check_therapist_availability()`,
`ohm#j4m8v2xq`, 2026-09-02) — a therapist with a matching row for a
booking's date can no longer be saved via New Booking, Quick Walk-in, or
Change/Reassign Therapist. New Booking's dropdown also surfaces the status
as a suffix + disabled option. See [[bookings_state]] for the full detail;
this table's own RLS/columns are unchanged by that work.

## RLS

`therapist_day_off`: `staff_select` (`is_staff()`), `staff_insert`/`staff_delete` (`is_supervisor_or_above()`) — added `ohm#7k2m9x4p`, 2026-08-30 (`20260830000000_therapist_day_off_rls.sql`), matching the identity-keyed pattern from Settings' catalog RLS.

`therapists`: `public_select` (SELECT, `using (true)`, baseline), `staff_update` (`is_supervisor_or_above()`, added `ohm#7m2w9dxk`, 2026-09-01, `20260901190000_therapists_archive_rls.sql` — backs both Archive/Unarchive and Edit-rename), `staff_insert` (`is_supervisor_or_above()`, added `ohm#5v8n3ptc`, 2026-09-01, `20260901200000_therapists_insert_rls.sql` — unblocks the pre-existing `createTherapist()` insert). No DELETE policy — archive stays UPDATE-only, consistent with every other catalog table.

`therapist_absence`/`therapist_leave`: `staff_select` (`is_staff()`), `staff_insert` (`is_supervisor_or_above()`) — added `ohm#3f8q1w6z`, 2026-08-30 (`20260830024144_therapist_absence_leave_rls.sql`), same pattern. No UPDATE/DELETE policy on either — unmarking an absence/leave record isn't built (append-only for this phase).

`therapist_services`: `staff_select` (`is_staff()`), `staff_insert`/`staff_delete` (`is_supervisor_or_above()`) — added `ohm#9q4x1mwr`, 2026-09-01 (`20260901210000_therapist_services_rls.sql`), same pattern as `therapist_day_off`. No UPDATE policy — pure join table, PK is `(therapist_id, service_id)`. This closes out the 3-prompt "Therapist Roster — Investigate & Wire" sequence started at `ohm#7m2w9dxk` — every therapist-roster affordance (Add, Archive/Unarchive, Edit-rename, Day Off, Services Offered) now has real Supabase persistence and correct RLS.
